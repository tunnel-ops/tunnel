# Required
DOMAIN=your-domain.com

# Optional — defaults shown
PROXY_PORT=7999
HEALTH_PORT=7998
MAX_BODY_MB=10
READ_TIMEOUT=30s
WRITE_TIMEOUT=30s
IDLE_TIMEOUT=120s

# Comma-separated port overrides (replaces the built-in blocked list)
# BLOCKED_PORTS=22,25,3306,5432,6379,27017
